export enum CommonPropsStatus {
    Active = 'active',
    Archived = 'archived',
    Deleted = 'deleted',
    Draft = 'draft',
}

export enum TabDefType {
    Meta = 'meta',
    Compound = 'compound',
    SingleField = 'single_field',
    TextList = 'text_list',
    System = 'system',
    Lorebook = 'lorebook',
}

export enum WidgetDefType {
    Calendar = 'calendar',
    Container = 'container',
    Custom = 'custom',
    Dropdown = 'dropdown',
    Image = 'image',
    List = 'list',
    None = 'none',
    Number = 'number',
    Text = 'text',
    Textarea = 'textarea',
    Toggle = 'toggle',
}

